# Linux-Update-Every-New-Session-
updates for Linux elements,  other notes on IDE state


sudo apt-get update
sudo apt-get upgrade
sudo apt update && sudo apt upgrade -y

**Agent Strict mode is ON*********** 

Sequential Thinking MCP
